name: CI and Vercel deployment

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  code-quality-check:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "24"
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
        
    - name: Install dependencies
      run: |
        cd app
        npm ci
        
    - name: Run linting
      run: |
        cd app
        npm run lint
        
    - name: Run tests
      run: |
        cd app
        npm test

  web-quality-check:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "24"
    - name: Check browser game script
      run: node --check app/game.js
    - name: Check static site files
      run: |
        test -f app/index.html
        test -f app/style.css
        test -f src/images/background.png
        test -f src/audio/swim.wav
        test -f src/audio/die.wav
        test -f src/audio/score.wav
        test -f src/audio/highscore.wav
        test -f src/images/obstacle.png
        test -f src/images/octo1.png
        test -f src/images/octo2.png
        test -f src/images/octo3.png

  deploy-to-vercel:
    needs: [code-quality-check, web-quality-check]
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Install current Vercel CLI
      run: npm install --global vercel@latest
    - name: Verify Vercel authentication
      run: vercel whoami --token="$VERCEL_TOKEN"
      env:
        VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
    - name: Deploy to Vercel
      uses: BetaHuhn/deploy-to-vercel-action@v1
      with:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        PRODUCTION: true
        GITHUB_DEPLOYMENT: true
        ATTACH_COMMIT_METADATA: false
